//14. We need a function that can transform a number into a string. What ways of achieving this do you know
//a.
function numberToString(num) {
  return num.toString();
}
numberToString(20);

//b.
function numberToString(num) {
  let strNum = num + "";
  return strNum;
}
